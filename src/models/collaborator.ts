import Model from './generic';

export class CollaboratorModel extends Model {
  delete(projecId: number): void {
    //
    // Deletes a collaborator from a shared project
    //
    // TODO
  }
}

export class CollaboratorStateModel extends Model {
  // It's okay
}
